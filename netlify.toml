[build]
  publish = "docs/browser"
  command = ng build --output-path docs --base-href='./' --configuration production"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200